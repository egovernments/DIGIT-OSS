import React from "react";
import { useParams } from "react-router-dom";

const TransfereeDetails = ({ userType, formData, config }) => {
  return <React.Fragment>This is Transferee Details</React.Fragment>;
};

export default TransfereeDetails;
