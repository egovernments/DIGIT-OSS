import React from "react";
import { useParams } from "react-router-dom";

const MutationDetails = ({ userType, formData }) => {
  return <React.Fragment>This is Mutation Details</React.Fragment>;
};

export default MutationDetails;
