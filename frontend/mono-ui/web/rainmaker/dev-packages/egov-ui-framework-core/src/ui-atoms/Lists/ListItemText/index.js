import React from "react";
import ListItemText from '@material-ui/core/ListItemText';

const MihyListItemText=(props)=>{
  const {...rest}=props;
  return(
    <ListItemText {...rest}/>
  )
}

export default MihyListItemText;
