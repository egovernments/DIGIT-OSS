/*DROP TABLE IF EXISTS targetcollection;
 
CREATE TABLE targetcollection (
  Serial Number INT PRIMARY KEY,
  City Name VARCHAR(250) NOT NULL,
  Code VARCHAR(250) NOT NULL,
  Budget Proposed(in Lakhs) VARCHAR(250) NOT NULL,
  Actual Collection(in Lakhs) VARCHAR(250) NOT NULL
  % Actual Collection over Budgeted VARCHAR(250) NOT NULL
);
 
INSERT INTO targetcollection (Serial Number, City Name, Code, Budget Proposed(in Lakhs), Actual Collection(in Lakhs),% Actual Collection over Budgeted) VALUES
  (?,?,?,?,?,?)*/